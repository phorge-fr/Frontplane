apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: overlay-configs
    namespace: flux-system
spec:
    interval: 10m0s
    path: ./overlays/svc/configs
    sourceRef:
        kind: GitRepository
        name: flux-system
    prune: true
    wait: true
    timeout: 5m0s
    dependsOn:
        -   name: overlay-controllers
    decryption:
        provider: sops
        secretRef:
            name: sops-age
