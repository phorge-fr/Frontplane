apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: traefik-local
    namespace: traefik
spec:
    values:
        service:
            annotations:
                lbipam.cilium.io/ips: 10.2.0.10
