apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
    name: nfs
provisioner: nfs.csi.k8s.io
parameters:
    server: 10.4.0.1
    share: /mnt/main/csi-svc
mountOptions:
    - nfsvers=4.1
    - nolock
reclaimPolicy: Delete
volumeBindingMode: Immediate
